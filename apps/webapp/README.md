## webapp

Web dashboard

### Environment Variables

Set the following variables in your `.env` file before running the web client:

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_API_URL` – base URL for the API service (e.g. `http://localhost:3003/api`)
